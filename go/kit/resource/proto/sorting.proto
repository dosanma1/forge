syntax = "proto3";

package tb.resource;

option go_package = "github.com/dosanma1/forge/go/kit/resource/pb";

enum SortDirection {
  SORT_UNKNOWN = 0;
  SORT_ASC = 1;
  SORT_DESC = 2;
}

message Sort {
    map<string, SortDirection> params = 1;
}